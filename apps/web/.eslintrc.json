{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "MemberExpression[object.property.name='api'][property.name='sendMessage']",
        "message": "Raw bot.api.sendMessage is banned outside server/channels/telegram/ — route outbound messages through telegramAdapter.send() (CAD-207). See server/channels/types.ts."
      },
      {
        "selector": "JSXText[value=/[a-z0-9]+_[a-z0-9]+/]",
        "message": "Raw snake_case string in JSX children — looks like an enum key leaking into the UI (Wave 5 Bug 14). Route display strings through the formatters in lib/labels.ts (CAD-208)."
      },
      {
        "selector": "JSXExpressionContainer > Literal[value=/^[a-z0-9]+(_[a-z0-9]+)+$/]",
        "message": "Raw snake_case string literal rendered in JSX — looks like an enum key leaking into the UI (Wave 5 Bug 14). Route display strings through the formatters in lib/labels.ts (CAD-208)."
      }
    ]
  },
  "overrides": [
    {
      "files": ["server/channels/telegram/**"],
      "rules": {
        "no-restricted-syntax": "off"
      }
    }
  ]
}
