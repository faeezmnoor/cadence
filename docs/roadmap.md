# Cadence Roadmap (now / next / later)

> The **source of truth is Linear** (team `CAD-`). This is the in-repo narrative view, regenerated by `cadence-bookkeeping` at CLOSE. Last refreshed: 2026-06-19. Umbrella epic: **CAD-233** (GA Gap Closure). State: paid-GA engineering complete; launch gated by founder/human items, not code.

## Now — GA-critical (P0)
- **Stripe MY KYC** (founder) — the hard GA wall; everything else in billing is built.
- **14-day dogfood streak** — CAD-209 (14 consecutive clean personal briefs before public signup).
- **Advanced-tier eval gate** — close the +0.26 → `MIN_LEAD = 0.5` gap (CAD-222 / CAD-236; ADR 0012).
- ✅ Shipped: copy honesty (CAD-234), DuckDuckGo fallback (CAD-165). In flight: regression hardening (CAD-225).

## Next — prove the moat + clear the gate (P1)
- **Measure personalization lift** — instrument the self-learning moat (CAD-235).
- **Validate the Haiku judge** — promote from log-only once ρ≥0.7 + κ≥0.6 over ≥50 pairs (CAD-236; ADR 0012).
- **Provider-selection eval** — pick the Standard default web-search provider (CAD-232 / CAD-228; ADR 0011).

## Later — registry, channels, CI eval, deferred (P2 / P3)
- **WhatsApp channel adapter** — wire `send()` (CAD-237).
- **CI G-eval gate + golden datasets + canonical rubric** (CAD-238).
- **Custom mode + BYO keys** — deferred until ≥50 paying users (CAD-103; ADR 0010).
- Phase 6b connectors (CAD-123 family); video/infographic formats; Malay/Chinese languages.
